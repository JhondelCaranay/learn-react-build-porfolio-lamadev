installation
    npm install react@17.0.0 react-dom@17.0.0
    $ npx create-react-app <project name>

NOTES:
    clear all unnecessary files in public and src folder, only leaft index.html, app.js, index.js

START APP
    cd portfolio 
    npm start

GOOGLE FONTS
    paste link into public/index.html


INSTALL SCSS FOR CSS
    $ npm install node-sass


MATERIAL Ui ICONS
    $ npm install @mui/material @emotion/react @emotion/styled
    $ npm install @mui/icons-material

ITYPE 
    // text effect library
    https://www.npmjs.com/package/ityped
    $ npm install ityped

MEDIA QUERY
    According to the Worldwide Screen Resolution Stats (Jan 2020 – Jan 2021),
    the most commonly used resolutions across mobile, desktop, and tablet are:

    1920×1080 (8.89%)
    1366×768 (8.44%)
    360×640 (7.28%)

ADD TO GITHUB
    $ git init
    $ git add .
    $ git commit -m "first commit"

    REMOVE TESTING DEPENDENCIES AND WEB VITALS
        $ npm uninstall @testing-library/jest-dom @testing-library/react @testing-library/user-event web-vitals

    create a new repository on the command line
        echo "# react-porfolio-tuts-deploy-netlify" >> README.md
        git init
        git add README.md
        git commit -m "first commit"
        git branch -M main
        git remote add origin https://github.com/JhondelCaranay/react-porfolio-tuts-deploy-netlify.git
        git push -u origin main

    push an existing repository from the command line
        git remote add origin https://github.com/JhondelCaranay/react-porfolio-tuts-deploy-netlify.git
        git branch -M main
        git push -u origin main 

DEPLOY REACT TO GITHUB TO NETLIFY AND HEROKU TUTORIAL
    https://www.youtube.com/watch?v=AOqY6disSVI&t=163s

NETLIFY THIS SITE CAN'T BE REACH

UPDATE ON PRODUCTION
    git add .
    git commit -m "Updated"
    git push -u origin main 


    
